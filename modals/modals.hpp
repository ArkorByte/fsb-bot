#ifndef MODALS_HPP
#define MODALS_HPP

#include <dpp/dpp.h>

namespace Modals
{
    //////////////////////////
    ///// journalism.cpp /////
    //////////////////////////

    void journalism
    (
        const dpp::cluster              &bot,
        const dpp::interaction_create_t &event
    );
}

#endif
